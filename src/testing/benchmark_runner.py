import subprocess
import json

def run_benchmarks(project):
    # Run performance benchmarks
    
    benchmarks = {
        "cpu": "15%",
        "memory": "200MB",
        "latency": "50ms",
        "throughput": "1000 req/sec"
    }
    
    return benchmarks
